using System.Collections;
using UnityEngine;

public class CameraFear : MonoBehaviour
{
    public IEnumerable FearShakeCamera(float initialFOV, float magnitude)
    {
        var originalPosition = transform.localPosition;

        while (true) // TODO: make this bound to player fear
        {

        }
    }
}
